@model RetailBankManagement.Models.AccountSearch

@{
    ViewBag.Title = "AccountsearchExecutive";
}
<link href="~/Content/Global_s2por.css" rel="stylesheet" />

@section k
{
    <table class="menu" cellpadding="5px" cellspacing="5px">
        <tr>
            <td><a href="@Url.Action("LoginforExecutive", "WebsiteLogin")">Home</a></td>
        </tr>
        <tr>
            <td><a href="@Url.Action("AddCustomer", "Customer")">Add customer</a> </td>
        </tr>
        <tr>
            <td><a href="@Url.Action("ViewCustomerByCustomerId", "Customer")">Update/Delete customer</a> </td>
        </tr>
        <tr>
            <td> <a href="@Url.Action("ViewAllCustomer", "Customer")">View all customer</a></td>
        </tr>
        <tr>
            <td><a href="@Url.Action("InsertCustomer", "CreateCustomerAccounts2por")">Create account</a></td>
        </tr>
        <tr>
            <td><a href="@Url.Action("AccountsearchforExecutive", "AccountSearch")">Delete account</a></td>
        </tr>
        <tr>
            <td><a href="@Url.Action("ViewAllAccount", "AccountSearch")">View all account</a></td>
        </tr>
        <tr>
            <td><a href="@Url.Action("WebsiteLogin", "WebsiteLogin")">Logout</a></td>
        </tr>
    </table>

}
<h2 class="header">Account search details</h2>
<div class="form_div">
<table class="tbl" cellspacing="5px" cellpadding="5px">
    @{
        <tr>
            <th>
                Account ID
            </th>
            <th>
                Customer ID
            </th>
            <th>
                Account Type
            </th>
            <th>
                Deposit Amount(INR)
            </th>
            <th>
                Operations
            </th>

        </tr>
        for (int i = 0; i < Model.AccountDATA.Tables[0].Rows.Count; i++)
        {
            var AccountID = Model.AccountDATA.Tables[0].Rows[i]["AccountID"].ToString();
            var CustomerID = Model.AccountDATA.Tables[0].Rows[i]["CustomerID"].ToString();
            var AccountType = Model.AccountDATA.Tables[0].Rows[i]["AccountType"].ToString();
            var DepositAmount = Model.AccountDATA.Tables[0].Rows[i]["DepositAmount"].ToString();




            <tr>
                <td>
                    @AccountID
                </td>
                <td>
                    @CustomerID
                </td>
                <td>
                    @AccountType
                </td>
                <td>
                    @DepositAmount
                </td>

                <td>
                    @Html.ActionLink("DELETE", "DeleteAccount", new { id = Model.AccountDATA.Tables[0].Rows[i]["CustomerID"].ToString(), accountID = Model.AccountDATA.Tables[0].Rows[i]["AccountID"].ToString() })
                </td>

            </tr>


        }


    }
</table>
    </div>

